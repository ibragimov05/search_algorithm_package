name: search_algorithm_package
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/ibragimov05/search_algorithm_package

environment:
  sdk: ^3.4.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
